spring:
  application:
    name: service-product
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: secret
  boot: # configuracion para admin-server
    admin:
      client:
        url: http://localhost:8086